# Lumen 项目协作规则

## 基本约束

1. 只执行用户明确提出的任务，不自行扩展产品范围或实现假设中的能力。
2. 所有计划、文档、代码注释和回复默认使用简体中文；每次面向用户的回复以“老大”开头。
3. 任何编码前必须先给出简洁、可执行的计划，并说明关键决策的原因。
4. 目标、需求或验收标准存在会显著影响实现范围的歧义时，先请求澄清，不擅自选择产品方向。
5. 遇到问题先定位根本原因，不以掩盖问题的补丁替代修复；不为当前实际场景不可能发生的情况过度设计。
6. 修改任意文件前，重新读取其最新内容；认为目录为空前，先执行目录列表确认。
7. `.cursor/rules/` 存在时，必须读取其中全部 `.mdc` 文件并遵守其规则；不存在时，在工作说明中明确本轮没有可用的 `.mdc` 规则。
8. 在 Windows PowerShell 中调用 PowerShell 命令时，使用以下模板并指定 UTF-8 输出：

   ```powershell
   powershell.exe -NoProfile -ExecutionPolicy Bypass -Command "[Console]::OutputEncoding = [System.Text.Encoding]::UTF8; <命令>"
   ```
9. Web 前端的按钮、输入、选择、开关、滑块、卡片、提示、弹层、进度指示等通用底层 UI 必须使用项目既定的开源组件库及 `apps/web/src/app/ui.tsx` 共享入口；开源组件库已经提供能力时，禁止使用可见原生控件、自绘交互控件或自研底层替代。仅允许保留组件库无法替代的不可见平台适配层（例如文件选择 input），且用户可见入口仍必须由开源组件库承载。产品专属组合组件可以保留在对应业务边界内。

## 文档优先

`docs/architecture/` 是 Lumen 唯一公开、正式且必须提交 Git 的文档上下文。任何涉及代码修改、开发计划或架构思考的任务开始前：

1. 读取 [`docs/architecture/README.md`](docs/architecture/README.md)。
2. 按该索引加载与任务直接相关的已确认架构文档及其显式关联文档。
3. 若本机存在 `docs-local/README.md`，再按任务需要读取其中索引的个人计划、调研或报告；它不是公开架构的前提。
4. 不默认读取无关历史文档，也不把对话内容当作唯一长期依据。

根目录 [`README.md`](README.md) 只说明产品定位、核心理念和品牌表达；它不是实施过程中的架构决策记录替代品。

## 项目最终目标

Lumen 的最终目标是帮助用户在真实英文材料中完成“阅读、理解、积累、回忆、继续阅读”的学习循环，并逐步建立独立英文阅读能力。

AI 在产品中承担辅助理解与学习反馈的职责，不能替代用户阅读，不能让产品偏离真实语境学习，也不能将 Lumen 演变为全文代读工具、孤立背词工具或通用聊天平台。

Lumen 是独立产品。Web、桌面端及外部平台集成都应围绕同一套产品核心演进；任何特定宿主、模型供应商或客户端形态都不能成为核心领域的前提。

已确认的产品、技术架构与架构决策记录在 `docs/architecture/`；个人计划、调研、报告及实验记录仅保存在 `docs-local/`，不写入本文件。

## 文档与报告规则

公开架构文档的目录职责、命名、状态和加载规则以 [`docs/architecture/README.md`](docs/architecture/README.md) 为准；本地工作资产的导航以 `docs-local/README.md` 为准。

特别约束：

- 开始非局部开发前，在 `docs-local/plan/` 创建或更新对应计划文档。
- 架构、持久化、AI 行为或用户可见流程发生非机械变化时，同步更新 `docs/architecture/`。
- **计划或开发阶段完成时，不自动创建 `docs-local/report/`。** 仅当用户明确要求生成阶段性报告、交付报告或总结报告时，才创建或更新该目录中的文档。
- 未经用户要求，不以“完成报告”为名复制计划、架构或 Git 提交信息。
- `docs-local/` 必须被 Git 忽略；`docs/` 内的公开文档不得链接或依赖其中的文件。

## 验证与变更边界

1. 只运行与当前修改直接相关、且项目当前可用的验证；没有可运行验证时，明确说明未执行的原因。
2. 保留用户已有的未提交修改，不重置、覆盖或删除与当前任务无关的内容。
3. 不执行不可逆或扩大范围的操作，除非用户明确授权。
